const { Contact } = require("./contacts");
const { User } = require("./user");
const { schemas } = require("./contacts");
const { schemasUser } = require("./user");

module.exports = {
  Contact,
  User,
  schemas,
  schemasUser
};
